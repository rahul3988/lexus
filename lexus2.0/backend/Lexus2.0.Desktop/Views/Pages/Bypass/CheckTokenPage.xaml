<UserControl x:Class="Lexus2_0.Desktop.Views.Pages.Bypass.CheckTokenPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="{StaticResource BackgroundWhite}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="{StaticResource PrimaryBlue}" Padding="15,10">
            <TextBlock Text="Check Token" FontSize="18" FontWeight="Bold" Foreground="{StaticResource White}" FontFamily="Segoe UI"/>
        </Border>
        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="15">
            <StackPanel>
                <Border Background="{StaticResource BackgroundLightGrey}" Padding="20" CornerRadius="3" BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="1" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Token Validation" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryBlue}" FontFamily="Segoe UI" Margin="0,0,0,15"/>
                        
                        <TextBlock Text="Enter Token:" Foreground="{StaticResource TextDark}" FontWeight="SemiBold" FontFamily="Segoe UI" Margin="0,0,0,5"/>
                        <TextBox x:Name="TokenTextBox" Background="{StaticResource White}" Foreground="{StaticResource TextDark}" Padding="8,6" FontFamily="Segoe UI" BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="1" Margin="0,0,0,15"/>
                        
                        <Button x:Name="ValidateButton" Content="Validate Token" Style="{StaticResource PrimaryButton}" Click="ValidateButton_Click" HorizontalAlignment="Left" Padding="15,8" MinWidth="150" Margin="0,0,0,15"/>
                        
                        <Border Background="{StaticResource White}" Padding="15" CornerRadius="3" BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Token Status:" Foreground="{StaticResource TextDark}" FontWeight="SemiBold" FontFamily="Segoe UI" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TokenStatusTextBlock" Text="Not Validated" Foreground="{StaticResource TextDark}" FontFamily="Segoe UI" FontSize="14" Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Valid Count:" Foreground="{StaticResource TextDark}" FontWeight="SemiBold" FontFamily="Segoe UI" Margin="0,0,0,5"/>
                                <TextBlock x:Name="ValidCountTextBlock" Text="0" Foreground="{StaticResource SuccessGreen}" FontFamily="Segoe UI" FontSize="14" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
                
                <Border Background="{StaticResource BackgroundLightGrey}" Padding="20" CornerRadius="3" BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Token Information" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryBlue}" FontFamily="Segoe UI" Margin="0,0,0,15"/>
                        <TextBlock Text="Token validation checks the license token against the server. A valid token is required for full application functionality." 
                                  Foreground="{StaticResource TextDark}" FontFamily="Segoe UI" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

