<UserControl x:Class="Lexus2_0.Desktop.Views.Pages.Data.AddPaymentOptionPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="{StaticResource BackgroundWhite}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="{StaticResource PrimaryBlue}" Padding="15,10">
            <TextBlock Text="Add Payment Option" FontSize="18" FontWeight="Bold" Foreground="{StaticResource White}" FontFamily="Segoe UI"/>
        </Border>
        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="15">
            <StackPanel>
                <Border Background="{StaticResource BackgroundLightGrey}" Padding="15" CornerRadius="3" BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="1" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Payment Method Details" FontSize="14" FontWeight="Bold" Foreground="{StaticResource PrimaryBlue}" FontFamily="Segoe UI" Margin="0,0,0,10"/>
                        
                        <TextBlock Text="Name:" Foreground="{StaticResource TextDark}" FontWeight="SemiBold" FontFamily="Segoe UI" Margin="0,0,0,5"/>
                        <TextBox x:Name="NameTextBox" Background="{StaticResource White}" Foreground="{StaticResource TextDark}" Padding="8,6" FontFamily="Segoe UI" BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="1" Margin="0,0,0,15"/>
                        
                        <TextBlock Text="Type:" Foreground="{StaticResource TextDark}" FontWeight="SemiBold" FontFamily="Segoe UI" Margin="0,0,0,5"/>
                        <ComboBox x:Name="TypeComboBox" Background="{StaticResource White}" Foreground="{StaticResource TextDark}" FontFamily="Segoe UI" BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="1" Padding="8,6" Margin="0,0,0,15">
                            <ComboBoxItem Content="Credit Card" IsSelected="True"/>
                            <ComboBoxItem Content="Debit Card"/>
                            <ComboBoxItem Content="UPI"/>
                            <ComboBoxItem Content="Net Banking"/>
                        </ComboBox>
                        
                        <TextBlock Text="Gateway:" Foreground="{StaticResource TextDark}" FontWeight="SemiBold" FontFamily="Segoe UI" Margin="0,0,0,5"/>
                        <ComboBox x:Name="GatewayComboBox" Background="{StaticResource White}" Foreground="{StaticResource TextDark}" FontFamily="Segoe UI" BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="1" Padding="8,6" Margin="0,0,0,15">
                            <ComboBoxItem Content="IRCTC" IsSelected="True"/>
                            <ComboBoxItem Content="UPI"/>
                            <ComboBoxItem Content="PayTM"/>
                            <ComboBoxItem Content="PhonePe"/>
                        </ComboBox>
                        
                        <TextBlock Text="Bank Name:" Foreground="{StaticResource TextDark}" FontWeight="SemiBold" FontFamily="Segoe UI" Margin="0,0,0,5"/>
                        <TextBox x:Name="BankNameTextBox" Background="{StaticResource White}" Foreground="{StaticResource TextDark}" Padding="8,6" FontFamily="Segoe UI" BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="1" Margin="0,0,0,15"/>
                        
                        <TextBlock Text="Card Number (Last 4 digits):" Foreground="{StaticResource TextDark}" FontWeight="SemiBold" FontFamily="Segoe UI" Margin="0,0,0,5"/>
                        <TextBox x:Name="CardNumberTextBox" Background="{StaticResource White}" Foreground="{StaticResource TextDark}" Padding="8,6" FontFamily="Segoe UI" BorderBrush="{StaticResource PrimaryBlue}" BorderThickness="1" Margin="0,0,0,15" MaxLength="4"/>
                        
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" x:Name="IsPriorBankCheckBox" Content="Set as Prior Bank" Foreground="{StaticResource TextDark}" FontFamily="Segoe UI"/>
                            <CheckBox Grid.Column="2" x:Name="IsBackupBankCheckBox" Content="Set as Backup Bank" Foreground="{StaticResource TextDark}" FontFamily="Segoe UI"/>
                        </Grid>
                        
                        <Button Content="Add Payment Option" Style="{StaticResource PrimaryButton}" Click="AddButton_Click" HorizontalAlignment="Left" Padding="15,8" MinWidth="150"/>
                    </StackPanel>
                </Border>
                
                <TextBlock Text="Existing Payment Options" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryBlue}" FontFamily="Segoe UI" Margin="0,0,0,10"/>
                <DataGrid x:Name="PaymentOptionsGrid" AutoGenerateColumns="False" Background="{StaticResource White}" Foreground="{StaticResource TextDark}" BorderBrush="{StaticResource PrimaryBlue}" GridLinesVisibility="All" HeadersVisibility="Column" CanUserAddRows="False" CanUserDeleteRows="False" FontFamily="Segoe UI">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                        <DataGridTextColumn Header="Gateway" Binding="{Binding Gateway}" Width="100"/>
                        <DataGridTextColumn Header="Bank" Binding="{Binding BankName}" Width="120"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                        <DataGridTemplateColumn Header="Actions" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Delete" Style="{StaticResource SecondaryButton}" Click="DeletePayment_Click" Tag="{Binding}" Padding="8,4" FontSize="11"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
                            <Setter Property="Foreground" Value="{StaticResource White}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontFamily" Value="Segoe UI"/>
                            <Setter Property="Padding" Value="10,5"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                </DataGrid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

